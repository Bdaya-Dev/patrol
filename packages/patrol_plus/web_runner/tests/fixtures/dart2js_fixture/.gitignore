# Generated by `dart pub get` / `dart compile js`, produced fresh on every
# run of sourceMapResolver.dart2js.browser.test.ts (issue #28) so the
# fixture's compiled bundle + source map always match the ACTUAL installed
# dart2js/SDK — never checked in stale, never a mock.
.dart_tool/
web/main.dart.js
web/main.dart.js.map
web/main.dart.js.deps
