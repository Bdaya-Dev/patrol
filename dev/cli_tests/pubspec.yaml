# Modified by Bdaya-Dev from the original LeanCode Patrol source (Apache-2.0). See NOTICE.md.
name: cli_tests
description: Scripts to test patrol_cli_plus.

environment:
  sdk: '>=3.8.0 <4.0.0'

dependencies:
  path: ^1.8.3

dev_dependencies:
  leancode_lint: ^17.0.0
